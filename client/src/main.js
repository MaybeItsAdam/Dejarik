window.onload = function () {
  var r_players = [];
  var b_players = [];

  var canvas = document.getElementById("pcs")
  var pcs_context = canvas.getContext("2d");

  // load red players
  for (r=0; r<4; r++) {
    r_players[r] = {}
  }


  // load blue players
  for (b=0; b<4; r++) {
    b_players[b] = {}
  }
}
